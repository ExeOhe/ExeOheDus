# Create your own .env file and place it in the root directory of your project.
# This file should not be committed to version control. Ensure gitignore is set up correctly.
# Replace the placeholders with your actual API keys and other sensitive information.
# Ensure that you have the necessary environment variables set up for your application to run correctly.
# Example .env file content: KEYS NOT URL'S!
BITQUERY_API_KEY=your_api_key_here